 – Définition de `Color` et instance de `Eq`

```haskell
-- Définition du type Color
data Color = Red | Green | Blue

-- Implémentation manuelle de la classe Eq
instance Eq Color where
    Red   == Red   = True
    Green == Green = True
    Blue  == Blue  = True
    _     == _     = False

-- Fonction main pour tester
main :: IO ()
main = do
    print (Red == Red)     -- True
    print (Red == Green)   -- False
    print (Blue == Blue)   -- True
    print (Green == Blue)  -- False

